import { MultipleChoiceAnswer } from './multiple-choice-answer';

describe('AddMultipleChoiceAnswer', () => {
  it('should create an instance', () => {
    expect(new MultipleChoiceAnswer()).toBeTruthy();
  });
});
